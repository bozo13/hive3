import React from 'react'
import { Box, Typography } from '@material-ui/core';
import Layout from '../components/layout';

export default () => (
    <Layout>
        <Box>
            <Typography variant="h2">Oops, that's a 404</Typography>
        </Box>
    </Layout>
)